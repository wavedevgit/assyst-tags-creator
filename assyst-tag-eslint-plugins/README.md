# Assyst eslint plugins

This package provides plugins for linting, assyst tags have limitations that should be not exceeded

`fetch-calls-limit.js` - Gives an error when you reach more then 5 fetch calls (5 calls is limit)
